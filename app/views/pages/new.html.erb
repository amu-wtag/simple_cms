<%= link_to("<< Back to list", pages_path, :class => 'back-link') %>

<div class="pages new">
    <h1>Create page</h1>
    <%= form_for(@page, :url => pages_path, :method => 'post') do |f| %>
        <%= render partial: "form", locals: {fbuilder: f} %>
        <div class="form-buttons">
            <%= f.submit("Create Page") %>
        </div>
    <% end %>
</div>